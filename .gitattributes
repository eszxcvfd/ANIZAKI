# Normalize text files to LF by default for cross-platform consistency.
* text=auto eol=lf

# Keep Windows command script line endings compatible.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

