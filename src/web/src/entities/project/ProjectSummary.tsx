import { Card } from '../../shared/ui/Card'

export function ProjectSummary() {
  return (
    <Card title="Project summary">
      <p>
        This UI baseline mirrors the planned monorepo architecture for a .NET 8
        API and React web application.
      </p>
    </Card>
  )
}
